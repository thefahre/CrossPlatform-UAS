import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.wakuwaku.wakuplanner',
  appName: 'WakuPlanner',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
